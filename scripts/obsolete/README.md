# Obsolete Scripts

These scripts are kept only as historical references.

Do not use them for new Orion runs. The active symmetric workflow is:

```bash
python3 scripts/setup_cases.py
python3 scripts/submit_workflow.py
python3 scripts/workflow_status.py
```

Some obsolete scripts reference retired paths or old full-mesh conventions and are not
expected to run without manual repair.
